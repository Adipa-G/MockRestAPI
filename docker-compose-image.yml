version: '3.8'

services:
  mock-rest-server:
    image: adipa/mock-rest-server
    container_name: mock-rest-server
    ports:
      - 5000:5000
    env_file: ./example/docker.env
    volumes:
      - ./example/MockCalls:/app/MockCalls
      - ./example/Endpoints:/app/Endpoints
